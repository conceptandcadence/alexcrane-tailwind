<div id="page-content" class="tabs learn-template">
    <h1>{{ page.title }}</h1>
    <section class="content">
        <nav>
            <ul>

            </ul>
        </nav>
        <div class="text">

        </div>
    </section>
    <div class="tabs-base visually-hidden"> {{ page.content }}</div>
</div>